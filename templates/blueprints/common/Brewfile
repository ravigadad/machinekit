# Brewfile.common — installed on every machine, in addition to the
# prerequisite tools that `machinekit apply` installs directly.

# Add formulae and casks as you want them:
#   brew "ripgrep"
#   brew "fd"
#   brew "bat"
#   brew "eza"
#   brew "jq"
#   cask "1password-cli"
